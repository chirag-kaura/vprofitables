version: '3.8'

services:
  vprofitables:
    build: .
    container_name: vprofitables-engine
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - GANN_PORT=8080
    restart: unless-stopped
